# Script Notes

This project now reads runtime settings from environment variables.

Key variables:
- `MONGO_URI`
- `MONGO_DB`
- `MONGO_COLLECTION`
- `SPARK_OUTPUT_PATH`
- `MLFLOW_TRACKING_URI`

Use `.env.example` as the reference template and create a local `.env` for your machine.
