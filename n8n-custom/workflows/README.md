# n8n Workflow Samples

Import `churn_high_risk_notification.json` into n8n via:
- `Workflows` -> `Import from file`

Update the HTTP endpoint in the `Send Alert` node to your internal notification service.
